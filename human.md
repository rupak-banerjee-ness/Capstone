First select a db type conversion and master it

not only for tables, for views, etc. every schema needs to be converted to other database

the conversion logic maybe sql query should be generated by the agent

the database we using is offline

we may stream or in batches the data as it arrives to our database. as database is huge and streaing helps.
